<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <gradient
        android:angle="360"
        android:centerColor="@color/colorPlaylist"
        android:endColor="@color/colorLighter"
        android:startColor="@color/colorPlaylist"
        android:type="linear" />
    <corners

        android:radius="0dp" />
</shape>